# smart-enter.yazi

Smart Enter: directories are entered, everything else goes through `[open]` rules
(extract archives, run AppImages, open files).

```toml
{ on = "<Enter>", run = "plugin smart-enter", desc = "Smart open" },
```
